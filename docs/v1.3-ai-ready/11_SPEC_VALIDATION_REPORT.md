# V1.3 需求包校验报告

> 实现状态更新（2026-07-20）：本文件以下内容是编码前对需求包本身的历史校验。
> V1.3 源码现已落地；实际实现映射和当前可执行验证分别见
> `docs/V1.3_IMPLEMENTATION_REPORT.md` 与 `docs/VALIDATION.md`。Windows Release 门禁仍需在 Windows 构建机执行。

校验日期：2026-07-20  
文档版本：1.0.0  

## 已通过

- `manifest.json` 可解析，声明的 12 份权威 Markdown 文件全部存在。
- 文档包含 13 个按依赖排序的实现任务：T00–T12。
- QA 矩阵包含 128 个具编号用例，覆盖迁移、空间、任务、Native 安全、忽略规则、索引、增量、搜索、Agent、资源、UI 与发布。
- 全部 Markdown 代码围栏成对闭合。
- 目标 SQLite 表与索引 DDL 在启用 FTS5 的内存数据库执行成功，共建立 36 个表/虚拟表/索引对象。
- 目标 DDL `foreign_key_check` 通过。
- 未发现旧截止时间字段、四列看板或只阻止未完成任务删除空间等已废弃决策。
- 需求包明确标记为 `requirements_only_not_implemented`，不会把计划能力误报为当前 V1.2 已实现。

## 特别验证项

校验环境自带 SQLite 3.50.4，仅用于 DDL 语法验证。根据 SQLite 官方 WAL 修复信息，该版本不满足 V1.3 并发索引运行要求；实现 T12 时必须确认应用随包 SQLite 为 `>=3.51.3` 或官方修复回移版本 `3.50.7/3.44.6`，并加入运行时和 CI 检查。

## 尚未执行

本次任务只编写开发需求，不包含 V1.3 代码。因此未执行：

- V1.2→V1.3 实际迁移。
- C++ scan ABI 构建与安全测试。
- WinUI 空间、任务和资产页面运行。
- 10,000/100,000 文件性能测试。
- Windows 安装器升级测试。

这些验证已分别分配到 T01、T03、T05、T07、T09、T10、T11 和 T12，不得在实现前标记通过。
