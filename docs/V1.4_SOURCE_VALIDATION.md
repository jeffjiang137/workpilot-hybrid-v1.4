# V1.4 源码验证记录

日期：2026-07-20  
环境：Linux x64 + 临时 .NET SDK 8.0.418

## 已通过

| 门禁 | 结果 |
|---|---|
| 托管 Models/Services Release 编译（warnings-as-errors） | PASS |
| `WorkPilot.Logic.Tests` | PASS |
| `WorkPilot.Integration.Tests`（升级、幂等、技能安装、恶意 ZIP 拒绝） | PASS |
| Migration 014、015、016 独立执行 | PASS |
| SQLite `quick_check` / `foreign_key_check` | PASS |
| 17 个 XAML/工程 XML 解析 | PASS |
| 2 个 JSON 清单解析 | PASS |
| 产品源码 TODO/NotImplemented/空 catch 扫描 | PASS |

托管服务编译项目为 `tests/WorkPilot.ServiceCompile.Tests`，Windows 一键构建也会执行该门禁。

## 当前环境不能完成

- MSVC x64 C++20 编译和 Native 单元测试。
- WinUI 3 XAML 编译与真实窗口运行。
- DPAPI、Windows Job Object、OAuth loopback 与 stdio MCP 的 Windows 集成测试。
- Inno Setup 安装、覆盖升级、卸载和干净 VM 冒烟。

尝试在 Linux 调用 WinUI Build 时，Windows 专用 `XamlCompiler.exe` 返回 `Exec format error`。这属于平台限制，不计为通过；必须在 Windows x64 运行根目录 `build-installer.bat`。脚本任一步失败都不会生成可分发安装器。
