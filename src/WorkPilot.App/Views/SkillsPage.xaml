<Page x:Class="WorkPilot.Views.SkillsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="using:WorkPilot.Models">
  <Grid Background="{StaticResource AppBackgroundBrush}" Padding="24" RowSpacing="16">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel><TextBlock Text="技能" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="导入纯声明式工作方法与知识包" Foreground="#A8A8A8"/></StackPanel>
      <Button Grid.Column="1" Content="导入技能包" Click="OnImport" Background="{StaticResource AccentGreenBrush}"/>
    </Grid>
    <InfoBar x:Name="InfoBar" Grid.Row="1" IsOpen="False" IsClosable="True"/>
    <Grid Grid.Row="2" ColumnSpacing="18"><Grid.ColumnDefinitions><ColumnDefinition Width="380"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <ListView x:Name="SkillList" SelectionChanged="OnSelected">
        <ListView.ItemTemplate><DataTemplate x:DataType="models:Skill"><Border Background="{StaticResource CardBrush}" CornerRadius="10" Padding="14" Margin="0,0,0,8">
          <StackPanel Spacing="4"><TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold"/><TextBlock Text="{x:Bind DisplayVersion}" Foreground="#999"/>
            <TextBlock Text="{x:Bind Description}" MaxLines="2" TextTrimming="CharacterEllipsis"/></StackPanel></Border></DataTemplate></ListView.ItemTemplate>
      </ListView>
      <Border Grid.Column="1" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="24">
        <StackPanel Spacing="12"><TextBlock x:Name="TitleText" Text="选择一个技能" FontSize="24" FontWeight="SemiBold"/>
          <TextBlock x:Name="DescriptionText" TextWrapping="Wrap"/><TextBlock x:Name="MetadataText" Foreground="#999" TextWrapping="Wrap"/>
          <StackPanel Orientation="Horizontal" Spacing="8"><Button x:Name="ToggleButton" Content="启用 / 停用" Click="OnToggle" IsEnabled="False"/>
            <Button x:Name="DeleteButton" Content="卸载" Click="OnDelete" IsEnabled="False"/></StackPanel>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Page>
